import { FileDropDirectiveDirective } from './file-drop-directive.directive';

describe('FileDropDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new FileDropDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
